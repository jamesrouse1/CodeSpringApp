process {
    executor = 'slurm'
    queue = 'cpuq'
}

executor {
    queueSize = 20
}

singularity {
    enabled = true
    autoMounts = true
    cacheDir = System.getenv('NXF_SINGULARITY_CACHEDIR')
}

